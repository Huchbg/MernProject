import { CreateProductProps } from "../../collections";
import { createProductFormProps } from "./createProductFormProps";

export const createProductProps: CreateProductProps = {
  formProps: createProductFormProps,
  title: "Create Product",
};
