import { ProductsMainProps } from "../../sections";
import { createProductProps } from "./createProductProps";

export const productMainProps: ProductsMainProps = {
  buttonText: "Create Product",
  createProductProps: createProductProps,
};
