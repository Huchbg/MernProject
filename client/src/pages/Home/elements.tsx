import { ProductsMain as _ProductsMain } from "../../sections";

import styled, { css } from "styled-components";

export const Home = styled.div(() => css``);

export const ProductsMain = styled(_ProductsMain)(() => css``);
